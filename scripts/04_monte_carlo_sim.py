# 04_monte_carlo_sim.py
# Use Monte Carlo simulations to estimate coupon collection time

# Import necessary libraries (numpy, pandas)
# Define a function `monte_carlo_ccp(n, num_simulations)`
#   - Run `num_simulations` trials
#   - Compute empirical mean of draws needed
#   - Compare Monte Carlo estimate to theoretical expectation
# Save results to `outputs/monte_carlo_results.csv`